<?php

require_once __DIR__.'/AppKernel.php';

use Eo\JobQueueBundle\Console\Application;
use Eo\JobQueueBundle\Tests\Functional\AppKernel;

$kernel = new AppKernel('persistent_db.yml');
$kernel->boot();

$app = new Application($kernel);
$app->run();